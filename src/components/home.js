import React from "react";
import Login from "../components/login"
import Register from "./register";

const Home = (prop) => {
    return (
        <div> 
            <h1>holis estoy usando react wii</h1>
            <Login/>
        </div>
    )
}

export default Home